{
  "name": "mission-control",
  "version": "0.0.1",
  "description": "Control plane for delegated agent runs",
  "type": "module",
  "bin": { "mc": "src/mc.ts" },
  "scripts": {
    "mc": "bun src/mc.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
