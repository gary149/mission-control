{
  "name": "mission-control",
  "version": "0.4.2",
  "description": "Control plane for delegated agent runs",
  "type": "module",
  "bin": { "mc": "dist/mc.js" },
  "files": ["dist"],
  "engines": { "node": ">=22.13" },
  "scripts": {
    "mc": "node src/mc.ts",
    "build": "tsc -p tsconfig.build.json && chmod +x dist/mc.js",
    "check": "tsc --noEmit",
    "test": "node --test test/e2e.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
